pipeline {
    agent any

    stages {
        stage('Execute shell script') {
            steps {
                script{
                    sh 'echo "Hello world from Jenkins server!"'
                }
            }
        }
    }
}
